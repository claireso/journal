import styled from 'styled-components'

const Tabs = styled.ul`
  border-bottom: 1px solid var(--secondary);
  display: inline-flex;
  list-style: none;
  margin: 0;
  padding: 0;
  width: 100%;
`

export default Tabs
