<%= form_for [:admin, @photo] do |f| %>
    <% if @photo.errors.any? %>
    <div class="error">
      <p><%= pluralize(@photo.errors.count, "error") %> prohibited this project from being saved:</p>
      <ul>
      <% @photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div>
        <%= f.label :title %>
        <%= f.text_field :title %>
    </div>
    <div>
        <%= f.label :file %>
        <%= f.file_field :file %>
    </div>
    <div>
        <div>
        <%= f.label :camera %>
        <%= f.text_field :camera %>
    </div>
    </div>
  <%= f.submit %>
<% end %>