import { pagesStyle, mb } from '@web/theme'

export const heading = pagesStyle({
  ...mb('size-6')
})

export const flash = pagesStyle({
  margin: 0,
  position: 'fixed',
  top: 0,
  left: 0,
  width: '100%'
})
