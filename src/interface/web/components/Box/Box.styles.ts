import { styled } from '@web/theme'

export default styled('div', {
  background: '$white',
  borderRadius: '$2',
  border: '1px solid $gray200',
  boxShadow: '$1',
  m: '0 auto',
  maxWidth: '40rem',
  p: 'calc($5 *2)'
})
